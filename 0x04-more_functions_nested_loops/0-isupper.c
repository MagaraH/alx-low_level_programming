#include "main.h"
/**
 * _isupper - function that checks for uppercase character
 *
 * @c: fuunction parameter
 * Return: 1 if is upper else 0
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (1);
	{
		return (0);
	}
}
