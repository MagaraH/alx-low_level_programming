#include "main.h"
/**
 * _isdigit - function that checks for digits 1 - 9
 *
 * @c: function parameter
 * Return: 1 if isdigit else 0
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
		return (1);
	{
		return (0);
	}
}
